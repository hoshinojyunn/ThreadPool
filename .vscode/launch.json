{
   "version": "0.2.0",
   "configurations": [
       {
          "name": "Debug",
          "type": "lldb",
          "request": "launch",
          "program": "${workspaceFolder}/build/bin/ThreadPool",
          "cwd": "${workspaceFolder}",
         //  "preLaunchTask": "build",
       }
    ]
}